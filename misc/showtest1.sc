//externals generated
//E (23663) task_wdt: Task watchdog got triggered. The following tasks did not reset the watchdog in time:

void main()
{
  int h=1;
  while(0==0)
  {
    for(int i=0;i<10;i++)
    {
    }
    show();
    h++;
  }
}